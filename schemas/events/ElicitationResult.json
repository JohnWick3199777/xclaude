{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ElicitationResult",
  "title": "ElicitationResult",
  "description": "Fired when the user answers an elicitation.",
  "properties": {
    "cwd": {
      "type": "string",
      "description": "Working directory of the session."
    },
    "hook_event_name": {
      "type": "string",
      "description": "Name of the event (e.g. SessionStart)."
    },
    "session_id": {
      "type": "string",
      "description": "Unique session identifier."
    },
    "transcript_path": {
      "type": "string",
      "description": "Path to the session transcript JSONL file."
    },
    "permission_mode": {
      "type": "string",
      "description": "Current permission mode."
    }
  },
  "additionalProperties": false,
  "required": [
    "cwd",
    "hook_event_name",
    "session_id",
    "transcript_path"
  ]
}
